// Deve restare identica alla stringa usata in firestore.rules (isAdmin()):
// le regole non possono importare questa costante, vanno aggiornate insieme.
export const ADMIN_EMAIL = 'turtlemind.dev@gmail.com';
